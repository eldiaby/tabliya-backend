# 🌐 The URL of the frontend client (used for CORS policy)
FRONTEND_ORIGIN=

# 🗄️ MongoDB Atlas credentials and URI
ATLAS_DB_USERNAME=         # Your MongoDB Atlas database username
ATLAS_DB_PASSWORD=         # Your MongoDB Atlas database password
ATLAS_DB_PROJECT=          # The name of your database/project
ATLAS_DB_URI=              # Full MongoDB Atlas connection URI

# 🗄️ Local MongoDB connection URI (for development)
LOCAL_DB_URI=              # e.g. mongodb://localhost:27017/matjar

# 🔐 JWT configuration for token generation and expiration
JWT_SECRET_KEY=            # A long, secure secret string for signing JWTs
JWT_LIFETIME=              # JWT token lifetime (e.g. 30d)
JWT_ACCESS_TOKEN_COOKIE_EXPIRES_IN=    # Access token cookie expiration (e.g. 1d)
JWT_REFRESH_TOKEN_COOKIE_EXPIRES_IN=   # Refresh token cookie expiration (e.g. 30d)


PASSWORD_RESET_TOKEN_EXPIRES_IN=


# 📧 Email configuration (e.g., Mailtrap or any SMTP provider)
EMAIL_HOST=                # SMTP server host (e.g. smtp.mailtrap.io)
EMAIL_PORT=                # SMTP server port (e.g. 587)
EMAIL_USERNAME=            # SMTP username
EMAIL_PASSWORD=            # SMTP password
EMAIL_FROM=                # Default from address (e.g. "Matjar <no-reply@matjar.com>")

# ⚙️ Application environment (development | production)
NODE_ENV=
